{
  "name": "@kojihq/lens",
  "version": "0.1.0-beta.0",
  "type": "module",
  "description": "Claude Code local usage analyzer CLI — koji-lens",
  "license": "MIT",
  "author": "Koji",
  "homepage": "https://github.com/etoryoki/koji-lens#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/etoryoki/koji-lens.git",
    "directory": "apps/cli"
  },
  "bugs": {
    "url": "https://github.com/etoryoki/koji-lens/issues"
  },
  "keywords": [
    "claude-code",
    "cli",
    "usage",
    "analytics",
    "koji-lens"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "bin": {
    "koji-lens": "./dist/index.js"
  },
  "files": [
    "dist",
    "web-standalone",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/bundle-web-standalone.mjs",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node ./dist/index.js",
    "pack:dry": "npm pack --dry-run"
  },
  "dependencies": {
    "@kojihq/core": "workspace:^0.1.0-beta.0",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
