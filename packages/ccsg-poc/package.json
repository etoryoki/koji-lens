{
  "name": "@kojihq/ccsg-poc",
  "version": "0.0.1-poc.0",
  "description": "PoC: Reversible masking for Claude Code Security Gateway. Day 45 evaluation only — not for production.",
  "private": true,
  "type": "module",
  "main": "./src/masker.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
